# This file is auto-generated by the Mailu configuration wizard.
# Please read the documentation before attempting any change.
# Generated for compose flavor

version: '2.2'

services:
  # External dependencies
  redis:
    # Has endpoint!
    image: redis:alpine
    restart: always
    volumes:
      - './var/mailu/redis:/data'

  # Core services
  front:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}nginx:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    logging:
      driver: json-file
    ports:
      - '127.0.0.1:80:80'
      - '127.0.0.1:443:443'
      - '127.0.0.1:25:25'
      - '127.0.0.1:465:465'
      - '127.0.0.1:587:587'
      - '127.0.0.1:110:110'
      - '127.0.0.1:995:995'
      - '127.0.0.1:143:143'
      - '127.0.0.1:993:993'
    volumes:
      - './var/mailu/certs:/certs'
      - './var/mailu/overrides/nginx:/overrides'

  admin:
    # Has endpoint!
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}admin:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    ports:
      - 127.0.0.1:8080:80
    volumes:
      - './var/mailu/data:/data'
      - './var/mailu/dkim:/dkim'
    depends_on:
      - redis

  imap:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}dovecot:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
      - './var/mailu/mail:/mail'
      - './var/mailu/overrides:/overrides'
    depends_on:
      - front

  smtp:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}postfix:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
      - './var/mailu/overrides:/overrides'
    depends_on:
      - front

  antispam:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}rspamd:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
      - './var/mailu/filter:/var/lib/rspamd'
      - './var/mailu/dkim:/dkim'
      - './var/mailu/overrides/rspamd:/etc/rspamd/override.d'
    depends_on:
      - front

  # Optional services
  antivirus:
    # Has endpoint!
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}clamav:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
      - './var/mailu/filter:/data'

  webdav:
    # Has endpoint!
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}radicale:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
      - './var/mailu/dav:/data'

  fetchmail:
    # Has endpoint!
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}fetchmail:${MAILU_VERSION:-1.7}
    restart: always
    env_file: ./mailu.env

  # Webmail
  webmail:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}roundcube:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
      - './var/mailu/webmail:/data'
    depends_on:
      - imap

  database:
    # Has endpoint!
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}postgresql:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
      - './var/mailu/data/psql_db:/data'
      - './var/mailu/data/psql_backup:/backup'

networks:
  default:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 192.168.203.0/24
